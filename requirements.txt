# =============================================================================
# requirements.txt
# Dépendances pour l'entraînement et l'évaluation.
# Versions épinglées pour la reproductibilité.
# =============================================================================

# --- Deep Learning ---
torch==2.1.0                    # PyTorch : framework DL principal
torchaudio==2.1.0               # Traitement audio (rééchantillonnage, transforms)

# --- HuggingFace Ecosystem ---
transformers==4.38.0            # Modèles pré-entraînés (WhisperForConditionalGeneration)
datasets==2.16.0                # Chargement et traitement des datasets (Arrow/CSV)
peft==0.8.0                     # Parameter-Efficient Fine-Tuning (LoRA)
evaluate==0.4.0                 # Métriques standard HuggingFace (WER, CER)

# --- Augmentation audio ---
audiomentations==0.37.0        # Augmentation audio (BandPassFilter, AddGaussianSNR)

# --- Métriques ASR ---
jiwer==3.0.3                    # Word Error Rate / Character Error Rate (backend de evaluate)

# --- MLOps ---
mlflow==2.10.0                  # Suivi d'expériences, Model Registry

# --- Configuration ---
hydra-core==1.3.2               # Gestion de configuration hiérarchique
omegaconf==2.3.0               # Interpolation de configs (utilisé par Hydra)

# --- Audio IO ---
librosa                         # Analyse audio, lecture de fichiers (version flexible)
soundfile                       # Lecture/écriture WAV (libsndfile backend)

# --- Export ---
optimum[onnxruntime]           # Export ONNX des modèles HuggingFace (optimum-cli)
ctranslate2                     # Runtime CTranslate2 (Faster-Whisper backend)
faster-whisper                  # Inférence optimisée Whisper avec CTranslate2

# --- Tests ---
pytest                          # Framework de tests unitaires
pytest-cov                      # Couverture de code avec pytest

# --- Versionning & Pipeline ---
dvc                             # Data Version Control + pipeline management

# --- Qualité de code ---
black                           # Formatage automatique du code Python
isort                           # Tri automatique des imports
